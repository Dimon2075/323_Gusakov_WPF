<Page x:Class="ISIP323_Gusakov_WPF.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISIP323_Gusakov_WPF"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Каталог товаров">
    <ScrollViewer>
        <ItemsControl x:Name="ProductItems" Margin="10">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5" Width="200">
                        <StackPanel>
                            <Image Source="{Binding ImagePath}" Height="100"/>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Text="{Binding Price, StringFormat='Цена: {0:C}'}"/>
                            <Button Content="Добавить в корзину" Margin="0,5" Tag="{Binding}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
